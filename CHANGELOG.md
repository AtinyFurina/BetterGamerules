# Changelog

## v1.1.0 (2026-08-02)

### Bug 修复

- **修复高级模式搜索框不生效**：过滤器设置的 `visible = false` 被渲染循环强制覆盖，导致搜索输入无效
- **修复自定义规则列表无法持久化**：双重 Bug——（1）`defineList` 验证器对 String 元素错误地检查 `instanceof List`，导致所有自定义配置被拒绝回退默认值；（2）`setSimpleModeRules()` 缺少 `SPEC.save()` 调用，修改从未写入磁盘
- **修复滑块单击不同步到服务器**：单击滑块（无拖拽）时 `sendUpdatePacket()` 从未被调用，修改只在客户端生效，重开界面后丢失

### 新功能

- **支持其他模组的游戏规则**：不再仅限原版规则。模组添加的整数规则自动获得动态滑块范围（不再硬编码 `[0, 1000]`），名称降级为格式化后的规则 ID（下划线转空格、驼峰分词），描述翻译缺失时显示通用提示
- 输入框宽度根据数值位数动态计算，大数字不会溢出

### 性能优化

- **搜索过滤不再重建全部 widget**：输入每个字符时不再销毁/重建所有条目控件，改为在渲染中过滤可见性
- **显示名称和描述预计算缓存**：消除每帧 `Component.translatable().getString()` 调用
- `CustomizeRulesScreen` 选中查找从 O(n) 降为 O(1)（`HashSet` 替代 `List.contains()`）

### 代码质量

- 消除 5 份重复的 `clamp()` 工具方法，统一到 `GameruleHelper`
- 统合分散在 4 个文件中的共享颜色常量
- 提取共享滚动条绘制方法
- 删除约 50 行死代码（无调用方法、未使用字段、冗余事件注册）
- 网络数据包添加字符串长度限制和 count 上限校验

---

## v1.0.0 (2026-07)

### 初始发布

- 简易模式 / 高级模式双标签界面
- 47 条原版游戏规则可视化编辑
- 布尔规则一键切换，整数规则滑块+输入框双向同步
- 中文完整汉化（规则名称+描述）
- 英文 UI 文本（本版本缺少规则英文翻译）
- 自定义简易模式规则列表
- Ctrl+G 快捷键 + `/bettergamerules` / `/bg` 客户端命令
- 多人服务器权限检查（需 OP 等级 2）
